---
const caseStudies = [
  {
    client: "Synthesis × Global FMCG Client",
    description: "Behaviour-shift programme for 30+ marketing managers across 8 APAC markets. 2-month co-design, 2-day immersive workshop. Shifted a team stuck in tool-mode to use of self.",
    quotes: [
      "A safe space for introverts.",
      "The confidence was already inside."
    ]
  },
  {
    client: "Synthesis (internal)",
    description: "4-part integration series for 20-30 junior strategists. Message, Messenger, Stage, Trigger. From brilliant analysis to felt resonance.",
    quotes: [
      "A breath of fresh air. Your style is so different and valuable to our young team."
    ]
  }
];
---

<section class="px-6 py-24 md:py-32 bg-off-white">
  <div class="max-w-4xl mx-auto">
    <div class="space-y-16">
      {caseStudies.map((study) => (
        <div class="animate-on-scroll">
          <h3 class="font-serif text-2xl md:text-3xl font-bold mb-6">
            {study.client}
          </h3>
          <p class="text-lg text-warm-gray mb-8 leading-relaxed">
            {study.description}
          </p>
          <div class="space-y-4">
            {study.quotes.map((quote) => (
              <blockquote class="quote text-lg">
                "{quote}"
              </blockquote>
            ))}
          </div>
        </div>
      ))}
    </div>
  </div>
</section>
